[user] 
name = Ys
email = vincent.vv.mys@gmail.com 
[core]
excludesfile = ~/.config/git/gitignore
editor = nvim
pager = delta

[merge]
tool = vimdiff
conflictstyle = diff3
[mergetool]
keepBackup = false
[mergetool "vimdiff"]
cmd = nvim -d $LOCAL $REMOTE $MERGED -c '$wincmd w' -c 'wincmd J'

[diff]
colorMoved = default
[interactive]
diffFilter = delta --color-only
[delta]
side-by-side = true

#WARN:!!! PASSWORD
[credential]
helper = store

#NOTE:
#[url "git@github.com:"]
#insteadOf = https://github.com/

#[http "https://github.com"]
#proxy = socks5://127.0.0.1:7890
